{
  "name": "linkedin-group-lister",
  "version": "0.1.0",
  "private": true,
  "description": "Group LinkedIn profiles into named lists, saved to Google Sheets.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3"
  }
}
